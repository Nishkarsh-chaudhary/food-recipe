import React from 'react'

const Reciepes = (props) => {
  const {reciepes} = props;
  return (
    <>
        {/* <section className='row'>
            {
              reciepes.map((reciepe,index) => (
                <div key={index} className='col-md-3'>
                  <div className="card">
                    <div className="card-body">
                      <h4>{reciepe.label}</h4>
                    </div>
                  </div>
                </div>
              ))
            }
        </section> */}

    </>
  )
}

export default Reciepes
